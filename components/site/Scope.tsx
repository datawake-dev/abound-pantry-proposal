import NotReady from "./_NotReady";

export default function Scope() {
  return (
    <section id="scope" aria-labelledby="scope-h2">
      <h2 id="scope-h2" className="sr-only">
        What the first release delivers.
      </h2>
      <NotReady name="Scope" />
    </section>
  );
}
