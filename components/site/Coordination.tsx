import NotReady from "./_NotReady";

export default function Coordination() {
  return (
    <section id="coordination" aria-labelledby="coordination-h2">
      <h2 id="coordination-h2" className="sr-only">
        Pantries stay in their inbox.
      </h2>
      <NotReady name="Coordination" />
    </section>
  );
}
