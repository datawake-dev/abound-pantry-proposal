import NotReady from "./_NotReady";

export default function CaseManager() {
  return (
    <section id="case-manager" aria-labelledby="case-manager-h2">
      <h2 id="case-manager-h2" className="sr-only">
        A case manager finds the right pantry in under a minute.
      </h2>
      <NotReady name="CaseManager" />
    </section>
  );
}
