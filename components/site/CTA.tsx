import NotReady from "./_NotReady";

export default function CTA() {
  return (
    <section id="cta-band" aria-labelledby="cta-h2">
      <h2 id="cta-h2" className="sr-only">
        Let us build this together.
      </h2>
      <NotReady name="CTA" />
    </section>
  );
}
