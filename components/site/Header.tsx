import NotReady from "./_NotReady";

// Floating Fluid Island nav — implemented in Task 24. This stub preserves the
// landmark so page-skeleton smoke tests and a11y tests find <header>.
export default function Header() {
  return (
    <header>
      <NotReady name="Header (Floating Fluid Island)" />
    </header>
  );
}
