import NotReady from "./_NotReady";

export default function Team() {
  return (
    <section id="team" aria-labelledby="team-h2">
      <h2 id="team-h2" className="sr-only">
        Three partners, three specific roles.
      </h2>
      <NotReady name="Team" />
    </section>
  );
}
